server:
  port: 6666
logging:
  level:
    org:
      springframework:
        security:
          oauth2: debug
security:
  oauth2:
    resource:
      jwt:
        key-store: classpath:mytest.jks
        key-alias: mytest
        key-password: mypass
        key-store-password: mypass
fxk:
  oauth2:
    token-store-type: redis
    resource:
      id: order